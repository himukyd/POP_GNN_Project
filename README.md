# POP_GNN_Project
GPU-accelerated graph coarsening pipeline for scalable GNN training. Reduces large graphs via CUDA heavy-edge matching, then trains GraphSAGE on the coarsened graph with full-graph test evaluation.
